@AGENTS.md

<!--
Gemini CLI reads GEMINI.md by default. The canonical instructions live in
AGENTS.md (imported above). If your Gemini CLI is configured with
contextFileName=AGENTS.md, this file is redundant and can be deleted.

Gemini has no equivalent of the block-compound-bash hook by default — the
command-discipline rule in AGENTS.md is therefore self-enforced here. To hard-
enforce it, port the hook to a Gemini BeforeTool hook (see portable/gemini/).
-->
